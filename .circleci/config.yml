version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/node:17.2.0 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello world" # run the `echo` command
      - run: echo "CIRCLE_PROJECT_USERNAME:" $CIRCLE_PROJECT_USERNAME
      - run: echo "CIRCLE_PROJECT_REPONAME:" $CIRCLE_PROJECT_REPONAME

workflows:
  my-workflow:
    jobs:
      - hello-job
